version: "3.9"
services:
  backend:
    build: ../backend
    container_name: timingguard-api
    ports: ["8000:8000"]
    restart: unless-stopped
  frontend:
    build: ../frontend
    container_name: timingguard-ui
    ports: ["3000:80"]
    depends_on: [backend]
    restart: unless-stopped
